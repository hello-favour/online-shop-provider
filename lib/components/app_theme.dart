import 'package:flutter/animation.dart';
import 'package:flutter/cupertino.dart';

class AppTheme {
  static const Color bgrdColor = Color(0xFFE2E2E2);

  //fontSize
  static const double fontSize = 40;

  //padding
  static const double bottomNavPadding = 8.0;
  static const BorderRadius appRadius = BorderRadius.all(Radius.circular(16));
}
